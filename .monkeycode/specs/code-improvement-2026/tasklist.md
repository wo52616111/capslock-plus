# CapsLock+ 代码改进实施计划

## 概述
本实施计划基于 2026-02-15 的代码审查结果，按照优先级和依赖关系组织改进任务。

- [ ] 1. 修复 Excel 批量插入行列逻辑错误
  - 修改 `!f` 热键，将 `EntireColumn.Insert` 改为 `EntireRow.Insert`
  - 修改 `!d` 热键，将 `EntireRow.Insert` 改为 `EntireColumn.Insert`
  - 移除不必要的 `Offset(1, 0)` 调用
  - 测试批量插入行功能是否正确
  - 测试批量插入列功能是否正确

- [ ] 2. 添加 InputBox 输入验证
  - [ ] 2.1 为批量插入行添加输入验证
    - 在 `!f` 热键中添加输入验证
    - 确保输入为正整数
    - 添加错误提示

  - [ ] 2.2 为批量插入列添加输入验证
    - 在 `!d` 热键中添加输入验证
    - 确保输入为正整数
    - 添加错误提示

- [ ] 3. 修复翻译 API 条件判断错误
  - 修改 `lib_ydTrans.ahk:20` 的条件判断
  - 将 `CLSets.TTranslate.appPaidID != "" && CLSets.TTranslate.appPaidID != ""` 改为 `CLSets.TTranslate.appPaidID != "" && CLSets.TTranslate.appPaidKey != ""`

- [ ] 4. 清理调试代码
  - 删除 `lib_clQ.ahk:8` 的 `msgbox, % p_l` 调试代码

- [ ] 5. 清理注释代码
  - 清理 `CapsLock+.ahk:27` 的注释代码
  - 清理 `CapsLock+.ahk:103-105` 的注释代码
  - 清理 `CapsLock+.ahk:346-356` 的注释代码
  - 清理 `CapsLock+.ahk:361-399` 的注释代码

- [ ] 6. 优化 COM 对象资源管理
  - [ ] 6.1 为 Excel 相关函数添加 COM 对象释放
    - 在 `!l` 热键结束时释放 `objExcel` 对象
    - 在 `!q` 热键结束时释放 `objExcel` 对象
    - 在 `!a` 热键结束时释放 `objExcel` 对象
    - 在 `!g` 热键结束时释放 `objExcel` 对象
    - 在 `!x` 热键结束时释放 `ox` 对象
    - 在 `!z` 热键结束时释放 `ox` 对象
    - 在 `!f` 热键结束时释放 `objExcel` 对象
    - 在 `!d` 热键结束时释放 `objExcel` 对象

- [ ] 7. 添加网络请求超时设置
  - 在 `lib_ydTrans.ahk:136-137` 的 HTTP 请求中添加超时设置
  - 设置合理的超时时间（建议 10 秒）

- [ ] 8. 添加边界检查配置
  - 将鼠标速度边界值（1-20）移至配置文件
  - 修改 `lib_mouseSpeed.ahk` 中的硬编码值
  - 修改 `lib_keysFunction.ahk` 中的硬编码值

- [ ] 9. 完善函数文档注释
  - [ ] 9.1 为 lib_functions.ahk 中的函数添加注释
    - 为 `getSelText()` 添加参数说明和返回值说明
    - 为 `UTF8encode()` 添加参数说明和返回值说明
    - 为 `URLencode()` 添加参数说明和返回值说明
    - 为 `checkStrType()` 添加参数说明和返回值说明
    - 为 `fixDpi()` 添加参数说明和返回值说明
    - 为 `setSettings()` 添加参数说明和返回值说明
    - 为 `showMsg()` 添加参数说明和返回值说明
    - 为 `extractSetStr()` 添加参数说明和返回值说明

  - [ ] 9.2 为 lib_keysFunction.ahk 中的关键函数添加注释
    - 为 `keyFunc_mouseSpeedIncrease()` 添加参数说明和返回值说明
    - 为 `keyFunc_mouseSpeedDecrease()` 添加参数说明和返回值说明
    - 为 `keyFunc_translate()` 添加参数说明和返回值说明
    - 为 `keyFunc_pasteSystem()` 添加参数说明和返回值说明

  - [ ] 9.3 为 lib_ydTrans.ahk 中的函数添加注释
    - 为 `ydTranslate()` 添加参数说明和返回值说明
    - 为 `ydApi()` 添加参数说明和返回值说明

  - [ ] 9.4 为 Excel 相关热键添加注释
    - 为 `!f` 热键添加功能说明
    - 为 `!d` 热键添加功能说明
    - 为 `!x` 热键添加功能说明
    - 为 `!z` 热键添加功能说明

- [ ] 10. 添加错误处理
  - [ ] 10.1 为文件操作添加错误处理
    - 在 `lib_settings.ahk:26-57` 添加 try-catch 错误处理
    - 在文件读取失败时显示友好的错误信息

  - [ ] 10.2 为网络请求添加错误处理
    - 在 `lib_ydTrans.ahk:145-150` 的网络请求中添加详细的错误处理
    - 区分不同类型的网络错误
    - 显示用户友好的错误信息

- [ ] 11. 优化配置管理
  - 将 API 密钥从代码中移至配置文件
  - 将硬编码的超时时间移至配置文件
  - 将硬编码的 DPI 值移至配置文件

- [ ] 12. 统一代码风格
  - 统一使用 `:=` 赋值符号
  - 统一缩进为 4 空格
  - 统一注释语言为中文

- [ ] 13. 检查点 - 确保所有修改正确
  - 确保所有高优先级和中优先级任务完成
  - 测试所有修改的功能是否正常工作
  - 如有疑问请询问用户

- [ ] 14. 评估管理员权限提升风险
  - [ ] 14.1 评估当前权限提升的必要性
    - 分析哪些功能需要管理员权限
    - 评估是否可以降低权限要求

  - [ ] 14.2 设计权限管理方案
    - 如果可以降低权限，修改权限提升逻辑
    - 如果必须使用管理员权限，添加 UAC 提示

- [ ] 15. 减少全局变量
  - [ ] 15.1 识别可以封装的全局变量
    - 分析所有全局变量的使用范围
    - 识别哪些变量可以封装到对象中

  - [ ] 15.2 创建配置对象
    - 创建 `Config` 对象封装配置相关变量
    - 创建 `State` 对象封装状态相关变量

  - [ ] 15.3 重构代码使用对象
    - 修改函数使用 `Config` 对象
    - 修改函数使用 `State` 对象

- [ ] 16. 编写单元测试
  - [ ] 16.1 编编码函数测试
    - 为 `UTF8encode()` 编写单元测试
    - 为 `URLencode()` 编写单元测试

  - [ ] 16.2 编写字符串类型检查测试
    - 为 `checkStrType()` 编写单元测试

  - [ ] 16.3 编写 DPI 修复函数测试
    - 为 `fixDpi()` 编写单元测试

  - [ ] 16.4 编写配置提取函数测试
    - 为 `extractSetStr()` 编写单元测试

- [ ] 17. 编写集成测试
  - [ ] 17.1 编写 Excel 功能集成测试
    - 测试批量插入行功能
    - 测试批量插入列功能
    - 测试单元格格式设置功能

  - [ ] 17.2 编写翻译功能集成测试
    - 测试有道翻译 API 调用
    - 测试错误处理机制

  - [ ] 17.3 编写计算板功能集成测试
    - 测试基本计算功能
    - 测试特殊输入处理

- [ ] 18. 更新文档
  - [ ] 18.1 更新 README.md
    - 添加新功能说明
    - 更新配置说明
    - 添加故障排除指南

  - [ ] 18.2 创建开发者文档
    - 创建 CONTRIBUTING.md
    - 创建代码规范文档
    - 创建 API 文档

- [ ] 19. 检查点 - 最终验证
  - 确保所有任务完成
  - 运行所有测试
  - 确保代码质量符合标准
  - 如有疑问请询问用户
